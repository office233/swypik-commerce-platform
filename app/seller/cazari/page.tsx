import StaysCalendarClient from "./StaysCalendarClient";

export const metadata = { title: "Calendar cazări — Swypik" };

export default function StaysCalendarPage() {
  return <StaysCalendarClient />;
}
