# Audit E2E complet — 2026-08-02 (3 agenți)

## Verdict pe module

| Modul | Verdict | Blocant principal |
|---|---|---|
| Shop cumpărător (browse→coș→checkout→comenzi→recenzii→retur) | 🟢 FUNCȚIONAL | — |
| SWYP economy (wallet/mining/staking/transfer/depozit/retragere on-chain) | 🟢 FUNCȚIONAL E2E | tx_hash real prin viem ✅ |
| Video pipeline (upload→transcodare HLS→publicare→feed→interacțiuni) | 🟢 aproape complet | lipsesc view-milestones |
| Food | 🟡 PARȚIAL | CRUD meniu merchant incomplet; dispatch-worker trebuie să ruleze |
| Stays | 🟡 PARȚIAL | plată DOAR din wallet (fără card); check-in digital absent |
| Go (rides) | 🟡 PARȚIAL | pricing_zones fără seed → 422 la orice cursă; statusuri intermediare șofer |
| Seller | 🟡 PARȚIAL | payouts (Stripe Connect off — amânat, cont Stripe lipsă) |
| Live (MediaMTX) | 🟡 PARȚIAL | lipsesc hooks on_publish/done → streamuri rămân „live" etern |
| Missions | 🟡 PARȚIAL | lipsesc submit/claim/award |
| Social | 🟡 PARȚIAL | follow/notificări in-app OK; DM+push sub flag off (cod există) |
| Battles | 🔴 ABSENT | zero cod (rute+tabele de creat de la zero) |
| Dispatch engine | 🟢 REAL | condiție: dispatch-worker.mjs să ruleze continuu |

## BACKLOG FIX — ordonat pe prioritate

### P0 — Critice (rup fluxuri existente)
1. [ ] **SWYP hibrid nu se aplică la plată** — `create-intent` nu apelează `applySwypPayment()`; SWYP doar preview. Fix: branch în `app/api/checkout/create-intent/route.ts`.
2. [ ] **Seed pricing_zones** — fără zone, Go dă 422 la orice estimare. Fix: migrare/seed cu zona pilot.
3. [ ] **dispatch-worker.mjs în producție** — fără el ofertele nu expiră. Fix: serviciu systemd în WSL.
4. [ ] **Live hooks MediaMTX** — `POST /api/live/hook` pentru on_publish/on_publish_done.

### P1 — Fluxuri incomplete (features vizibile)
5. [ ] **View milestones** — awardSwyp la praguri de vizionări (100/1K/10K) în `/api/videos/[id]/view`.
6. [ ] **Missions submit/claim** — `POST /api/missions/[id]/submit` + review + award.
7. [ ] **Comenzile shop lipsesc din /orders** — ActivityClient arată doar Eats/Go.
8. [ ] **Stoc: decrementare la checkout** — verificat: EXISTA deja în webhook (`_handlers/checkout.ts` face UPDATE stock). Fals-pozitiv audit.
9. [ ] **CRUD meniu merchant complet** — PUT/DELETE menu-items + UI.
10. [ ] **Stays: plată card** — PaymentIntent Stripe pe lângă wallet.

### P2 — De activat/complet (cod există)
11. [ ] **Push notifications** — generat chei VAPID, flag on.
12. [ ] **DM** — flag on după test (SSE există).
13. [ ] **Statusuri intermediare Go** — arrived_pickup/in_progress pt șofer.
14. [ ] **Seller approve → email notificare**.
15. [ ] **Stays check-in digital** — cod/QR la rezervare.

### P3 — De construit (noi)
16. [ ] **Battles de la zero** (tabele + CRUD + cron close) — sau scoatem din UI până există.
17. [ ] **CDN în fața S3/MinIO pentru HLS** (avem deja cdn.swypik.com → MinIO prin tunel; de verificat cache headers).
18. [ ] **Chat live + tips în live**.
19. [ ] Stripe Connect payouts — DUPĂ cont Stripe (amânat explicit).

## Note operaționale
- Mediul: cod în E:\Meister\swypik\app (Windows), prod în WSL /opt/swypik/app, deploy = git pull + rebuild web-next.
- Auditul complet al agenților: rapoartele sunt sintetizate aici; detaliile per fișier în acest document la nevoie.
